#include <stdio.h>
int main()
{
	int T;
	int A, B;
	scanf("%d", &T);
	for (int i = 1; i <= T; i++)
	{
		scanf("%d %d", &A, &B);
		if (0 < A && B < 10)
			printf("Case #%d: %d + %d = %d\n", i, A, B, A + B);
	}
	return 0;
}
